import * as React from 'react';
import classnames from 'classnames';

import styles = require('./styles.scss');

const Footer: React.FC<{}> = () => {
  return <div className={styles['side-panel-Footer_Footer']}></div>;
};

export default Footer;

import * as React from 'react';
import classnames from 'classnames';

import styles = require('./styles.scss');

const Header: React.FC<{}> = () => {
  return <div className={styles['side-panel-header_header']}></div>;
};

export default Header;

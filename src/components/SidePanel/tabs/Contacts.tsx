import * as React from 'react';
import classnames from 'classnames';

import styles = require('../styles.scss');

const Contacts: React.FC<{}> = () => {
  const contacts = [{}];
  return <div className={styles['side-panel-Contacts_Contacts']} />;
};

export default Contacts;

import * as React from 'react';
import classnames from 'classnames';

import styles = require('./styles.scss');

const HeaderPanel: React.FC<{}> = () => {
  return <div className={styles['side-panel-header_header']} />;
};

export default HeaderPanel;
